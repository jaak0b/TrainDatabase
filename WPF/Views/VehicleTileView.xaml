<UserControl x:Class="Shell.WPF.Views.VehicleTileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance }">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="White" Height="240" Width="500" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Source="{Binding VehicleImage}" Stretch="Uniform"
               HorizontalAlignment="Left" VerticalAlignment="Top">
        </Image>
        <TextBlock Grid.Row="2" Text="{Binding Vehicle.Name}" IsHitTestVisible="False" FontWeight="Bold"></TextBlock>
        <Grid.Effect>
            <DropShadowEffect Opacity="0.2" RenderingBias="Quality" />
        </Grid.Effect>
    </Grid>
</UserControl>